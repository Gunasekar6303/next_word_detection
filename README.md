# next_word_detection
Next word detection using GRU.

Just download the code and install the requirements.txt file and hit "python main.py" in terminal for training. And "python testing.py" for testing the model in the terminal.
